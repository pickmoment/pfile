{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the main window, matching what Tauri grants implicitly when no capabilities file is present, plus webview zoom control for the UI scale setting.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:webview:allow-set-webview-zoom",
    "fs:default",
    "dialog:default",
    "opener:default"
  ]
}
